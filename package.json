{
  "name": "lighter",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the Lighter GNOME Shell extension (the extension itself has no runtime npm dependencies)",
  "scripts": {
    "lint": "eslint extension.js prefs.js lib preferences"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
